layers:
  - name: 'ne_10m_admin_0_antarctic_claim_limit_lines'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_antarctic_claim_limit_lines AS t
  - name: 'ne_10m_admin_0_antarctic_claims'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_antarctic_claims AS t
  - name: 'ne_10m_admin_0_boundary_lines_disputed_areas'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_boundary_lines_disputed_areas AS t
  - name: 'ne_10m_admin_0_boundary_lines_land'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_boundary_lines_land AS t
  - name: 'ne_10m_admin_0_boundary_lines_map_units'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_boundary_lines_map_units AS t
  - name: 'ne_10m_admin_0_boundary_lines_maritime_indicator'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_boundary_lines_maritime_indicator AS t
  - name: 'ne_10m_admin_0_countries'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_countries AS t
  - name: 'ne_10m_admin_0_countries_lakes'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_countries_lakes AS t
  - name: 'ne_10m_admin_0_disputed_areas'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_disputed_areas AS t
  - name: 'ne_10m_admin_0_disputed_areas_scale_rank_minor_islands'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_disputed_areas_scale_rank_minor_islands AS t
  - name: 'ne_10m_admin_0_label_points'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_label_points AS t
  - name: 'ne_10m_admin_0_map_subunits'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_map_subunits AS t
  - name: 'ne_10m_admin_0_map_units'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_map_units AS t
  - name: 'ne_10m_admin_0_pacific_groupings'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_pacific_groupings AS t
  - name: 'ne_10m_admin_0_scale_rank'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_scale_rank AS t
  - name: 'ne_10m_admin_0_scale_rank_minor_islands'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_scale_rank_minor_islands AS t
  - name: 'ne_10m_admin_0_seams'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_seams AS t
  - name: 'ne_10m_admin_0_sovereignty'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_0_sovereignty AS t
  - name: 'ne_10m_admin_1_label_points'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_label_points AS t
  - name: 'ne_10m_admin_1_label_points_details'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_label_points_details AS t
  - name: 'ne_10m_admin_1_seams'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_seams AS t
  - name: 'ne_10m_admin_1_states_provinces'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_states_provinces AS t
  - name: 'ne_10m_admin_1_states_provinces_lakes'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_states_provinces_lakes AS t
  - name: 'ne_10m_admin_1_states_provinces_lines'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_states_provinces_lines AS t
  - name: 'ne_10m_admin_1_states_provinces_scale_rank'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_states_provinces_scale_rank AS t
  - name: 'ne_10m_admin_1_states_provinces_scale_rank_minor_islands'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_admin_1_states_provinces_scale_rank_minor_islands AS t
  - name: 'ne_10m_airports'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_airports AS t
  - name: 'ne_10m_antarctic_ice_shelves_lines'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_antarctic_ice_shelves_lines AS t
  - name: 'ne_10m_antarctic_ice_shelves_polys'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_antarctic_ice_shelves_polys AS t
  - name: 'ne_10m_coastline'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_coastline AS t
  - name: 'ne_10m_geographic_lines'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_geographic_lines AS t
  - name: 'ne_10m_geography_marine_polys'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_geography_marine_polys AS t
  - name: 'ne_10m_geography_regions_elevation_points'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_geography_regions_elevation_points AS t
  - name: 'ne_10m_geography_regions_points'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_geography_regions_points AS t
  - name: 'ne_10m_geography_regions_polys'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_geography_regions_polys AS t
  - name: 'ne_10m_glaciated_areas'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_glaciated_areas AS t
  - name: 'ne_10m_lakes'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_lakes AS t
  - name: 'ne_10m_lakes_europe'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_lakes_europe AS t
  - name: 'ne_10m_lakes_historic'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_lakes_historic AS t
  - name: 'ne_10m_lakes_north_america'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_lakes_north_america AS t
  - name: 'ne_10m_lakes_pluvial'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_lakes_pluvial AS t
#  - name: 'ne_10m_land'
#    minZoom: 6
#    maxZoom: 13
#    queries:
#      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_land AS t
  - name: 'ne_10m_land_ocean_label_points'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_land_ocean_label_points AS t
  - name: 'ne_10m_land_ocean_seams'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_land_ocean_seams AS t
  - name: 'ne_10m_land_scale_rank'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_land_scale_rank AS t
  - name: 'ne_10m_minor_islands'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_minor_islands AS t
  - name: 'ne_10m_minor_islands_coastline'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_minor_islands_coastline AS t
  - name: 'ne_10m_minor_islands_label_points'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_minor_islands_label_points AS t
  - name: 'ne_10m_ocean'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_ocean AS t
  - name: 'ne_10m_ocean_scale_rank'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_ocean_scale_rank AS t
  - name: 'ne_10m_parks_and_protected_lands_area'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_parks_and_protected_lands_area AS t
  - name: 'ne_10m_parks_and_protected_lands_line'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_parks_and_protected_lands_line AS t
  - name: 'ne_10m_parks_and_protected_lands_point'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_parks_and_protected_lands_point AS t
  - name: 'ne_10m_parks_and_protected_lands_scale_rank'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_parks_and_protected_lands_scale_rank AS t
  - name: 'ne_10m_playas'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_playas AS t
  - name: 'ne_10m_populated_places'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_populated_places AS t
  - name: 'ne_10m_populated_places_simple'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_populated_places_simple AS t
  - name: 'ne_10m_ports'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_ports AS t
  - name: 'ne_10m_railroads'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_railroads AS t
  - name: 'ne_10m_railroads_north_america'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_railroads_north_america AS t
  - name: 'ne_10m_reefs'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_reefs AS t
  - name: 'ne_10m_rivers_europe'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_rivers_europe AS t
  - name: 'ne_10m_rivers_lake_centerlines'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_rivers_lake_centerlines AS t
  - name: 'ne_10m_rivers_lake_centerlines_scale_rank'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_rivers_lake_centerlines_scale_rank AS t
  - name: 'ne_10m_rivers_north_america'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_rivers_north_america AS t
  - name: 'ne_10m_roads'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_roads AS t
  - name: 'ne_10m_roads_north_america'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_roads_north_america AS t
  - name: 'ne_10m_time_zones'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_time_zones AS t
  - name: 'ne_10m_urban_areas'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_urban_areas AS t
  - name: 'ne_10m_urban_areas_landscan'
    minZoom: 6
    maxZoom: 13
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_10m_urban_areas_landscan AS t
  - name: 'ne_110m_admin_0_boundary_lines_land'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_boundary_lines_land AS t
  - name: 'ne_110m_admin_0_countries'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_countries AS t
  - name: 'ne_110m_admin_0_countries_lakes'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_countries_lakes AS t
  - name: 'ne_110m_admin_0_map_units'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_map_units AS t
  - name: 'ne_110m_admin_0_pacific_groupings'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_pacific_groupings AS t
  - name: 'ne_110m_admin_0_scale_rank'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_scale_rank AS t
  - name: 'ne_110m_admin_0_sovereignty'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_sovereignty AS t
  - name: 'ne_110m_admin_0_tiny_countries'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_0_tiny_countries AS t
  - name: 'ne_110m_admin_1_states_provinces'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_1_states_provinces AS t
  - name: 'ne_110m_admin_1_states_provinces_lakes'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_1_states_provinces_lakes AS t
  - name: 'ne_110m_admin_1_states_provinces_lines'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_1_states_provinces_lines AS t
  - name: 'ne_110m_admin_1_states_provinces_scale_rank'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_admin_1_states_provinces_scale_rank AS t
  - name: 'ne_110m_coastline'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_coastline AS t
  - name: 'ne_110m_geographic_lines'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_geographic_lines AS t
  - name: 'ne_110m_geography_marine_polys'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_geography_marine_polys AS t
  - name: 'ne_110m_geography_regions_elevation_points'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_geography_regions_elevation_points AS t
  - name: 'ne_110m_geography_regions_points'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_geography_regions_points AS t
  - name: 'ne_110m_geography_regions_polys'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_geography_regions_polys AS t
  - name: 'ne_110m_glaciated_areas'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_glaciated_areas AS t
  - name: 'ne_110m_lakes'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_lakes AS t
  - name: 'ne_110m_land'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_land AS t
  - name: 'ne_110m_ocean'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_ocean AS t
  - name: 'ne_110m_populated_places'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_populated_places AS t
  - name: 'ne_110m_populated_places_simple'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_populated_places_simple AS t
  - name: 'ne_110m_rivers_lake_centerlines'
    minZoom: 0
    maxZoom: 3
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_110m_rivers_lake_centerlines AS t
  - name: 'ne_50m_admin_0_boundary_lines_disputed_areas'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_boundary_lines_disputed_areas AS t
  - name: 'ne_50m_admin_0_boundary_lines_land'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_boundary_lines_land AS t
  - name: 'ne_50m_admin_0_boundary_lines_maritime_indicator'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_boundary_lines_maritime_indicator AS t
  - name: 'ne_50m_admin_0_boundary_map_units'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_boundary_map_units AS t
  - name: 'ne_50m_admin_0_breakaway_disputed_areas'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_breakaway_disputed_areas AS t
  - name: 'ne_50m_admin_0_breakaway_disputed_areas_scale_rank'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_breakaway_disputed_areas_scale_rank AS t
  - name: 'ne_50m_admin_0_countries'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_countries AS t
  - name: 'ne_50m_admin_0_countries_lakes'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_countries_lakes AS t
  - name: 'ne_50m_admin_0_map_subunits'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_map_subunits AS t
  - name: 'ne_50m_admin_0_map_units'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_map_units AS t
  - name: 'ne_50m_admin_0_pacific_groupings'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_pacific_groupings AS t
  - name: 'ne_50m_admin_0_scale_rank'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_scale_rank AS t
  - name: 'ne_50m_admin_0_sovereignty'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_sovereignty AS t
  - name: 'ne_50m_admin_0_tiny_countries'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_tiny_countries AS t
  - name: 'ne_50m_admin_0_tiny_countries_scale_rank'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_0_tiny_countries_scale_rank AS t
  - name: 'ne_50m_admin_1_states_provinces'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_1_states_provinces AS t
  - name: 'ne_50m_admin_1_states_provinces_lakes'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_1_states_provinces_lakes AS t
  - name: 'ne_50m_admin_1_states_provinces_lines'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_1_states_provinces_lines AS t
  - name: 'ne_50m_admin_1_states_provinces_scale_rank'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_admin_1_states_provinces_scale_rank AS t
  - name: 'ne_50m_airports'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_airports AS t
  - name: 'ne_50m_antarctic_ice_shelves_lines'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_antarctic_ice_shelves_lines AS t
  - name: 'ne_50m_antarctic_ice_shelves_polys'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_antarctic_ice_shelves_polys AS t
  - name: 'ne_50m_coastline'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_coastline AS t
  - name: 'ne_50m_geographic_lines'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_geographic_lines AS t
  - name: 'ne_50m_geography_marine_polys'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_geography_marine_polys AS t
  - name: 'ne_50m_geography_regions_elevation_points'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_geography_regions_elevation_points AS t
  - name: 'ne_50m_geography_regions_points'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_geography_regions_points AS t
  - name: 'ne_50m_geography_regions_polys'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_geography_regions_polys AS t
  - name: 'ne_50m_glaciated_areas'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_glaciated_areas AS t
  - name: 'ne_50m_lakes'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_lakes AS t
  - name: 'ne_50m_lakes_historic'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_lakes_historic AS t
  - name: 'ne_50m_land'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_land AS t
  - name: 'ne_50m_ocean'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_ocean AS t
  - name: 'ne_50m_playas'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_playas AS t
  - name: 'ne_50m_populated_places'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_populated_places AS t
  - name: 'ne_50m_populated_places_simple'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_populated_places_simple AS t
  - name: 'ne_50m_ports'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_ports AS t
  - name: 'ne_50m_rivers_lake_centerlines'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_rivers_lake_centerlines AS t
  - name: 'ne_50m_rivers_lake_centerlines_scale_rank'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_rivers_lake_centerlines_scale_rank AS t
  - name: 'ne_50m_urban_areas'
    minZoom: 4
    maxZoom: 5
    queries:
      - sql: SELECT ogc_fid as id, hstore(t.*) as tags, geom FROM ne_50m_urban_areas AS t
  - name: 'aeroway'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'aeroway'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'aeroway'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'aeroway' AND tags -> 'type' = 'multipolygon'
  - name: 'waterway'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'waterway'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'waterway'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'waterway' AND tags -> 'type' = 'multipolygon'
  - name: 'landuse'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'landuse'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'landuse'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'landuse' AND tags -> 'type' = 'multipolygon'
  - name: 'railway'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'railway'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'railway'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'railway' AND tags -> 'type' = 'multipolygon'
  - name: 'highway'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'highway'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'highway'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'highway' AND tags -> 'type' = 'multipolygon'
  - name: 'geological'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'geological'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'geological'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'geological' AND tags -> 'type' = 'multipolygon'
  - name: 'public_transport'
    minZoom: 12
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'public_transport'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'public_transport'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'public_transport' AND tags -> 'type' = 'multipolygon'
  - name: 'aerialway'
    minZoom: 12
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'aerialway'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'aerialway'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'aerialway' AND tags -> 'type' = 'multipolygon'
  - name: 'geological'
    minZoom: 12
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'geological'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'geological'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'geological' AND tags -> 'type' = 'multipolygon'
  - name: 'building'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'building'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'building'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'building' AND tags -> 'type' = 'multipolygon'
  - name: 'amenity'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'amenity'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'amenity'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'amenity' AND tags -> 'type' = 'multipolygon'
  - name: 'craft'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'craft'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'craft'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'craft' AND tags -> 'type' = 'multipolygon'
  - name: 'emergency'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'emergency'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'emergency'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'emergency' AND tags -> 'type' = 'multipolygon'
  - name: 'historic'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'historic'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'historic'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'historic' AND tags -> 'type' = 'multipolygon'
  - name: 'leisure'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'leisure'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'leisure'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'leisure' AND tags -> 'type' = 'multipolygon'
  - name: 'man_made'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'man_made'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'man_made'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'man_made' AND tags -> 'type' = 'multipolygon'
  - name: 'military'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'military'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'military'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'military' AND tags -> 'type' = 'multipolygon'
  - name: 'natural'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'natural'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'natural'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'natural' AND tags -> 'type' = 'multipolygon'
  - name: 'office'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'office'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'office'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'office' AND tags -> 'type' = 'multipolygon'
  - name: 'place'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'place'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'place'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'place' AND tags -> 'type' = 'multipolygon'
  - name: 'power'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'power'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'power'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'power' AND tags -> 'type' = 'multipolygon'
  - name: 'route'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'route'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'route'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'route' AND tags -> 'type' = 'multipolygon'
  - name: 'shop'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'shop'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'shop'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'shop' AND tags -> 'type' = 'multipolygon'
  - name: 'sport'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'sport'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'sport'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'sport' AND tags -> 'type' = 'multipolygon'
  - name: 'telecom'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'telecom'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'telecom'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'telecom' AND tags -> 'type' = 'multipolygon'
  - name: 'tourism'
    minZoom: 14
    maxZoom: 14
    queries:
      - sql: SELECT id, tags, geom FROM osm_nodes WHERE tags ? 'tourism'
      - sql: SELECT id, tags, geom FROM osm_ways WHERE tags ? 'tourism'
      - sql: SELECT id, tags, geom FROM osm_relations WHERE tags ? 'tourism' AND tags -> 'type' = 'multipolygon'

